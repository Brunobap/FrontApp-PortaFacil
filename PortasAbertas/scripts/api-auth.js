import { userAdm } from "../constants/dummies";

export function CheckLogin (req){
    // Lógica da API pedir a confirmação do login vai aqui

    // Por enquanto só manda um usuário genérico
    return userAdm
}